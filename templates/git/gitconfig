[user]
    name = {{ .Name }}
    email = {{ .PersonalEmail }}
{{ range .Orgs }}
[includeIf "hasconfig:remote.*.url:**{{ .URL }}**"]
    path = {{ $.Home }}/.gitconfig-org-{{ .Name }}
{{ end }}
[core]
    hooksPath = ~/.config/git/hooks
